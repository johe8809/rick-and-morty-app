library com.rick_and_morty.ui_shared.gradients;

import 'package:flutter/material.dart' hide Colors;

class Gradients {
  static const LinearGradient main = LinearGradient(
    colors: <Color>[
      Color.fromRGBO(0, 245, 160, 1),
      Color.fromRGBO(0, 217, 245, 1),
    ],
  );
}
